apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

patches:
  # smaller resources for dev
  - target: { kind: Deployment }
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
